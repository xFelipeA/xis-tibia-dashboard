class InicioTab {
    constructor() {
        this.init();
    }

    init() {
        const tabElement = document.getElementById('inicio-tab');
        
        // This tab is already populated in the HTML
        // We can add dynamic content here if needed
        console.log('✅ Aba Início carregada');
    }
}
